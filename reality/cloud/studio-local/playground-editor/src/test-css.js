document.addEventListener('DOMContentLoaded', () => {
  const element = document.createElement('h2')
  element.textContent = 'CSS Test: Purple Header'
  element.className = 'css-header'
  document.body.appendChild(element)
})

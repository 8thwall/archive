---
id: persistent
---

import ComponentFunctions from "@site/src/components/component-functions";

# Persistent

## Beschreibung

Diese Komponente kennzeichnet die Entität, an die sie angehängt ist, als persistent, was bedeutet, dass sie beim Aushängen des Spaces (Wechseln von Spaces) nicht despawnt wird. Kann nur bei Root-Objekten gesetzt werden.

## Eigenschaften

Keine

## Funktionen

<ComponentFunctions
    name="Persistent"
    properties={{}}
    mutate={`return false`}
/>

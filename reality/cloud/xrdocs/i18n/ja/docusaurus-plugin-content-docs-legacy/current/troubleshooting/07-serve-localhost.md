---
id: serve-localhost
---

# serve "スクリプトに接続できない

#### 問題 {#issue}

サーブ」スクリプト（8th Wall Webの公開GitHubリポジトリ：
<https://github.com/8thwall/web>より）を使ってラップトップでローカルのウェブサーバーを動かしているのですが、
**127.0.0.1**でリスニングしていると表示されます。  私の携帯電話は、そのIPアドレスを使用してラップトップに接続することができません。  私の携帯電話は、そのIPアドレスを使用してラップトップに接続することができません。

![ServeLocalhost](/images/serve-localhost.jpg)

"127.0.0.1 "はラップトップのループバックアドレス（別名 "localhost"）なので、
電話などの他のデバイスはそのIPアドレスに直接接続することはできません。  何らかの理由で、`serve`スクリプトは
、ループバックインターフェースをリッスンすることにした。  何らかの理由で、`serve`スクリプトは
、ループバックインターフェースをリッスンすることにした。

#### 解像度 {#resolution}

serve`スクリプトを`-i\\` フラグを付けて再実行し、使用したいネットワーク・インターフェイスを指定してください。

#### 例 (Mac) {#example-mac}

./serve/bin/serve -d gettingstarted/xraframe/ -p 7777 -i en0\\`.

#### 例 (Windows) {#example-windows}

**注意:**標準の**コマンドプロンプト**ウィンドウ(**cmd.exe**)を使用して、以下のコマンドを実行してください。
\*スクリプトをPowerShellから実行するとエラーが発生します。

servebinserve.bat -d gettingstarted\xraframe -p 7777 -i WiFi\\`.

それでも接続できない場合は、以下をご確認ください：

- お使いのコンピューターとモバイルデバイスの両方が**同じWiFiネットワーク**に接続されていることを確認してください。
- **コンピュータで実行されているローカルの**ファイアウォール\*\*を無効にしてください。
- 接続するには、QRコードをスキャンするか、または**必ず**「リスニング」URL全体を
  、冒頭の「**https://**」と末尾の**ポート**番号の両方を含めて、ブラウザにコピーしてください。

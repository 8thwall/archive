---
id: self-hosted-domains
---

# セルフ・ホスティング・ドメイン

有料のProプランまたはEnterpriseプランをご利用の場合、Web AR体験を
、ご自身のWebサーバーでホストすることができます（デバイスの認証なしで見ることができます）。
そのためには、
プロジェクトのホストとして承認されているドメインのリストを指定する必要があります。
そのためには、
プロジェクトのホストとして承認されているドメインのリストを指定する必要があります。

1. プロジェクト・ダッシュボード・ページから、"Setup domains "を選択する。

2. Setup this project for self-hosting or local development "を展開する。

3. プロジェクトをセルフホスト（
   ）するウェブサーバーのドメイン（複数可）またはIP（複数可）を入力します。 ドメインにワイルドカード、パス、ポートを含めることはできない。 をクリック
   複数追加する。

注：セルフホスト・ドメインは**サブドメイン固有**です。例えば、`mydomain.com`は
`www.mydomain.com`と同じではありません。 注：セルフホスト・ドメインは**サブドメイン固有**です。例えば、`mydomain.com`は
`www.mydomain.com`と同じではありません。 mydomain.comと`www.mydomain.com`の両方でホスティングする場合は、
**BOTH**を指定する必要があります。 注：セルフホスト・ドメインは**サブドメイン固有**です。例えば、`mydomain.com`は
`www.mydomain.com`と同じではありません。 mydomain.comと`www.mydomain.com`の両方でホスティングする場合は、
**BOTH**を指定する必要があります。

![SelfHostedDomainList](/images/console-app-key-origins.jpg)

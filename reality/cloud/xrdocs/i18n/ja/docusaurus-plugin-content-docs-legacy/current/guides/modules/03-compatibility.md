# モジュールの互換性 {#module-compatibility}

8thウォールモジュールは、8thウォールホスティングプロジェクトのみ、セルフホスティングプロジェクトのみ、
、または8thウォールホスティングプロジェクトとセルフホスティングプロジェクトの両方で利用可能です。 デフォルトのモジュールは、8th Wall
ホストされたプロジェクトとセルフホストされたプロジェクトの両方で利用可能です。 これはモジュールの設定ページで変更できる。 デフォルトのモジュールは、8th Wall
ホストされたプロジェクトとセルフホストされたプロジェクトの両方で利用可能です。 これはモジュールの設定ページで変更できる。

![module-compatibility-settings](/images/modules-compatibility-settings.jpg)

モジュールは8th Wallホストとセルフホスト両方のプロジェクトで動作するように適切にコーディングされなければなりません。
では、モジュール内から参照される特定のアセットは、
自主ドメインにロードできるように、クロスオリジン有効としてロードされなければなりません。 例えば、
メディアに明示的にクロスオリジン属性を設定する：
では、モジュール内から参照される特定のアセットは、
自主ドメインにロードできるように、クロスオリジン有効としてロードされなければなりません。 例えば、
メディアに明示的にクロスオリジン属性を設定する：

![module-example-cors](/images/modules-example-cors.jpg)

あるいは、ウェブ ワーカーを呼び出す前に fetch を使ってウェブ ワーカー blob をプリロードする：

![module-example-webworker](/images/modules-example-webworker.jpg)
---
id: follow-animation
---

import ComponentFunctions from "@site/src/components/component-functions";

# フォローアニメーション

## 商品説明

このコンポーネントは、エンティティにアニメーションを作成します。

## プロパティ一覧

| Property    | Type   | 初期値       | 商品説明                                                                                                                |
| ----------- | ------ | --------- | ------------------------------------------------------------------------------------------------------------------- |
| ターゲット       | イード    | 未定義       | アニメートする対象のオブジェクト。 指定されていない場合、アニメーションはコンポーネントがアタッチされているオブジェクト上で実行されます。                                               |
| 最小距離        | 番号     | 0         | 追従時に維持すべき最短距離（メートル単位）。                                                                                              |
| 最大距離        | 番号     | 0         | 追従時に維持すべき最大距離（メートル単位）。                                                                                              |
| 弾力性         | 番号     | 1         | ターゲットが最小／最大距離の外にいるときに移動する距離の割合（1秒あたり）（0は移動せず、1は瞬時に移動する）                                                             |

## 関数一覧

<ComponentFunctions
    name="FollowAnimation"
    properties={{
        minDistance: 0,
        maxDistance: 0,
        elasticity: 1
    }}
    mutate={`cursor.minDistance = 5;
    cursor.elasticity = 0.5;
    return false;`}
/>

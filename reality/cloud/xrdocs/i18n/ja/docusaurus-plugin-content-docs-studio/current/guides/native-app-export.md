---
id: native-app-export
description: このセクションでは、Native App Exportの使用方法について説明します。
---

# ネイティブアプリのエクスポート

:::info[Beta 特集］
ネイティブアプリのエクスポートは現在ベータ版で、Androidビルドに限定されています。 iOS、デスクトップ、ヘッドセットへの対応は近日中。
:::

Native App Exportを使用すると、Studioプロジェクトをスタンドアロンのアプリケーションとしてパッケージ化できます。

---

## 必要条件

ネイティブエクスポートは、非AR、3Dのみのプロジェクトでのみ利用可能です。 あなたのプロジェクトは、**使用してはならない**：

- カメラまたはAR機能
- GPS
- ランドスケープモード
- 仮想または物理キーボード
- ゲームパッド
- 振動
- プッシュ通知
- アプリ内課金
- ビデオテクスチャ
- MediaRecorder API
- 健康データ
- ウェブエックスアール
- CSS

> エクスポートを試みる前に、プロジェクトが少なくとも一度は正常にビルドされていることを確認してください。

---

## アンドロイドへのエクスポート

1. Studioプロジェクトを開きます。
   プロジェクトが上記の要件基準を満たしていることを確認する。

2. Publish**をクリックしてください。
   Build for Native Platforms**で、\*\*Android (Beta)\*\*を選択します。

3. アプリのビルドをカスタマイズ：
   - **アプリ名:** Androidのホーム画面に表示される名前
   - **Bundle Identifier:** `com.mycompany.myapp` などの一意な文字列。
   - \*アプリのアイコン（1024x1024）をアップロードしてください。

4. アプリの基本情報を入力したら、**Continue**をクリックしてビルド設定を確定します。

---

## ビルド設定の最終決定

次に、アプリをどのようにパッケージ化するかを定義する：

- **バージョン名：** セマンティック・バージョニングを使用する（例：`1.0.0`）。
- **向き:**
  - **縦向き:** 端末を回転させたときでもアプリを垂直位置に固定します。
  - **横向き:** アプリを横向きにして左側を下げます。
  - **横向き右:** アプリを水平方向に表示し、右側が下がるようにします。
  - **自動回転:** アプリがデバイスの物理的な回転に従い、垂直ビューと水平ビューを自動的に切り替えることができます。
  - **自動回転 (Landscape Only):** デバイスの回転に基づいてアプリの位置を調整しますが、水平ビューのみに制限します。
- **ステータスバー:**
  - **はい:** アプリケーション上にデフォルトのシステムステータスバーを表示します。
  - **いいえ:** 既定のシステムステータスバーを非表示にします。
- \*\*輸出タイプ
  - \*\*APK（Androidパッケージ）：\*\*テストまたはサイドローディングのための直接インストールファイル
  - **AAB（Android App Bundle）：** Google Playでの公開に必要です。
- \*\*ビルド・モード
  - **スタティック・バンドル:** 完全自己完結型ビルド
  - \*\*ライブリロード：\*\*\*プロジェクトが更新されると、スタジオから更新をプルします。
- **環境:** `Dev`、`Latest`、`Staging`、`Production` から選択する。

すべての設定が完了したら、**Build**をクリックしてアプリ・パッケージを生成します。

> ビルドが完了したら、ビルドの概要に記載されているダウンロードリンクを使って `.apk` または `.aab` ファイルをダウンロードしてください。

---

## Google Playストアへの公開

エクスポートが完了したら、\*\*AAB (Android App Bundle)\*\*を使ってアプリをPlayストアに公開する準備が整いました：

### なぜAABなのか？

Googleは2021年8月以降、すべての新規アプリにAABフォーマットを要求している。AABは、デバイス固有のAPKを生成し、アプリのサイズを縮小することで、配信を最適化するのに役立つ。

### Google Play Consoleにアップロードする

1. Play Console](https://play.google.com/console)にログインし、必要に応じてPlay App Signingに登録する。
2. アプリの作成 "\*\*に移動→名前、言語、無料/有料ステータスを入力します。
3. テスト＆リリース\*\* → **プロダクション**（または内部/ベータトラック）に移動します。 **Create new release**をクリックし、.aabファイルを**Drop app bundles here to upload**セクションにドラッグしてアップロードしてください。
4. 完全な店舗リスト、プライバシーポリシー、コンテンツ評価、対象地域
5. リリースのレビューとロールアウト

🔗 [アップロードに関する完全なドキュメントはこちら: Play Consoleへのアプリのアップロード](https://developer.android.com/studio/publish)

---

## Androidデバイスに直接インストールする

### Androidエミュレータへのインストール

1. ブラウザやファイルマネージャーで、\*\*「不明なアプリをインストールする」\*\*を有効にする。
2. USB、Eメール、クラウドストレージ経由でAPKを転送する。
3. お使いのデバイスからAPKを開き、**インストール**をタップします。

**コマンドライン方式の場合：**\*。

```bash
adb install path/to/app.apk
```

### 物理的なAndroidデバイスへのインストール

1. Android StudioのAVD Managerでエミュレーターをセットアップする。
2. エミュレータを実行する。
3. お使いのデバイスからAPKを開き、**インストール**をタップします。

ターミナルで

```bash
adb install path/to/app.apk
```

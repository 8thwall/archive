import React from 'react'

/**
 * @deprecated This component used to do something "smart" with mapping `<input name="myState">`
 * to `this.state.myState`. None of it's callers are actually using that though.
 * It will be removed once camera-edit-page is deleted.
 */
const StateChangeComponent = React.Component

export default StateChangeComponent

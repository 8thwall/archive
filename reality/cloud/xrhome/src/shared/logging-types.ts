type RequestLogFields = {
  // eslint-disable-next-line camelcase
  app_key_source?: string
  // eslint-disable-next-line camelcase
  exclude_web_view?: boolean
}

export type {
  RequestLogFields,
}

diff --git a/node_modules/parse-domain/build-cjs/src/sanitize.js b/node_modules/parse-domain/build-cjs/src/sanitize.js
index 034a0d6..41238e9 100644
--- a/node_modules/parse-domain/build-cjs/src/sanitize.js
+++ b/node_modules/parse-domain/build-cjs/src/sanitize.js
@@ -101,7 +101,7 @@ exports.sanitize = (input) => {
     for (const label of labels) {
         // According to https://tools.ietf.org/html/rfc6761 labels should
         // only contain ASCII letters, digits and hyphens (LDH).
-        const invalidCharacter = /[^\d\-a-z]/iu.exec(label);
+        const invalidCharacter = /[^\d\-a-z_]/iu.exec(label);
         if (invalidCharacter) {
             labelValidationErrors.push(createLabelInvalidCharacterError(label, invalidCharacter[0], invalidCharacter.index + 1));
         }
diff --git a/node_modules/parse-domain/build-esm/src/sanitize.js b/node_modules/parse-domain/build-esm/src/sanitize.js
index f55c8cb..886dcb9 100644
--- a/node_modules/parse-domain/build-esm/src/sanitize.js
+++ b/node_modules/parse-domain/build-esm/src/sanitize.js
@@ -96,7 +96,7 @@ export const sanitize = (input) => {
     for (const label of labels) {
         // According to https://tools.ietf.org/html/rfc6761 labels should
         // only contain ASCII letters, digits and hyphens (LDH).
-        const invalidCharacter = /[^\d\-a-z]/iu.exec(label);
+        const invalidCharacter = /[^\d\-a-z_]/iu.exec(label);
         if (invalidCharacter) {
             labelValidationErrors.push(createLabelInvalidCharacterError(label, invalidCharacter[0], invalidCharacter.index + 1));
         }

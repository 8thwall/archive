import * as ecs from '@8thwall/ecs'
const PlanterTargetModel = ecs.registerComponent({
  name: 'PlanterTargetModel',
  schema: {},
  schemaDefaults: {},
  add: (world, component) => {},
  remove: (world, component) => {},
})

export {PlanterTargetModel}

import * as ecs from '@8thwall/ecs'
const GravityGunExit = ecs.registerComponent({
  name: 'GravityGunExit',
  schema: {},
  schemaDefaults: {},
  add: (world, component) => {},
  remove: (world, component) => {},
})

export {GravityGunExit}

import * as ecs from '@8thwall/ecs'
const GravityGunAffected = ecs.registerComponent({
  name: 'GravityGunAffected',
  schema: {},
  schemaDefaults: {},
  add: (world, component) => {},
  remove: (world, component) => {},
})

export {GravityGunAffected}

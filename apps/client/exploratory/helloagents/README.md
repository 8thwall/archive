# Welcome Hello Agents
## Setup Instructions
- First install the dependencies using `pip install -r requirements.txt`.
- Next, to generate the prompts run `./scripts/generate_prompts.py`. Optionally, use may use `--explain-code` flag to add explanations along with code generations.
- Finally, you can run `streamlit run src/main.py` to start the streamlit app locally.